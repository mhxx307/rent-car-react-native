import { StyleSheet, Text, View } from 'react-native';

const Menu = () => {
    return (
        <View>
            <Text>Navbar</Text>
        </View>
    );
};

export default Menu;

const styles = StyleSheet.create({});
