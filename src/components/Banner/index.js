import { StyleSheet, Text, View } from 'react-native';

const Banner = () => {
    return (
        <View>
            <Text>Banner</Text>
        </View>
    );
};

export default Banner;

const styles = StyleSheet.create({});
